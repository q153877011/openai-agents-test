[[index]]
url = "https://mirrors.tencent.com/pypi/simple/"
default = true

# 使用系统 CA 证书
native-tls = true

# 跳过以下主机的 TLS 证书验证（解决公司网络/VPN/杀毒软件的中间人拦截）
allow-insecure-host = [
    "mirrors.tencent.com",
    "pypi.org",
    "files.pythonhosted.org",
]
